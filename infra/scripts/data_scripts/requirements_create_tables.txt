# required for run_psql_create_tables_script.py only
psycopg2-binary==2.9.10
azure-identity==1.19.0
#azure-keyvault-secrets==4.5.0
#msal==1.24.0b1               #This exists in Azure CLI 2.52.0. No need to install it separately.
#msal-extensions==1.0.0       #This exists in Azure CLI 2.52.0. No need to install it separately.